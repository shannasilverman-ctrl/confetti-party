# Loop state

The durable machine-readable state is written to `STATE.json` when the harness
runs. That file is ignored so model output and local paths do not enter source
control. This document records the intended starting point:

- Status: ready to configure agent CLI adapters
- Maximum iterations: 4
- Deployment inside loop: prohibited
- Escalation: required after the fourth failed review
